# Release Checklist

Before pushing changes:

- confirm `npm run build` passes
- verify hero copy still leads with the person
- verify project links resolve correctly
- verify RETAIN AI screenshot still loads
- verify resume link works
- verify no section names drift from the agreed labels

After pushing changes:

- confirm the GitHub Pages workflow runs
- check the live site in a fresh tab
