# Dependency Notes

The current homepage uses a small runtime dependency set.

## Runtime dependencies

- `next`
- `react`
- `react-dom`
- `framer-motion`
- `lenis`
- `react-icons`

## Policy

- keep the runtime set small
- avoid adding libraries for one-off visual effects
- prefer local components over extra UI frameworks
